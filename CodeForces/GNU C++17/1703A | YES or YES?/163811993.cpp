#include "bits/stdc++.h"
#include <algorithm>
 
using namespace std ;
#define ll long long
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin>>t;
    while (t--){
 
        string su;
        cin>>su;
        transform(su.begin(), su.end(), su.begin(), ::toupper);
        if(su=="YES") cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
 
 
 
}
 
 
 
}
 
 
 
 
 
 
 